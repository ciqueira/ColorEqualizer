#ifndef MC_COLOR_EQUALIZER_MCOPENNEX_PLATFORM_H
#define MC_COLOR_EQUALIZER_MCOPENNEX_PLATFORM_H

namespace mcopen {

bool canOpenUrl(const char *url);
bool openUrl(const char *url);
bool openMCNexusApplication();

} // namespace mcopen

#endif
